<%= turbo_frame_tag :modal do %>
  <%= simple_form_for [@model_agency_profile] do |f| %>
    <%= f.input :user_id, placeholder: "User", collection: User.where(user_type: "mannequin"), label_method: :full_name, class: "m-2" %>
    <%= f.submit "+ PROFILE", class: "button-dashboard" %>
  <% end %>
<% end %>
